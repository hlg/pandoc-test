name: Pandoc release

on:
  push:
    tags:
      - v*
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Say hello
        run: echo Hello, world!
      - uses: docker://pandoc/latex:2.9
        with:
          args: -N --bibliography test.bib --standalone -o test.pdf  test.md
      - name: releasemsg
        run: |
          prevtag=$(git tag --sort version:refname | tail -n 2 | head -n 1)
          lasttag=$(git tag --sort version:refname | tail -n 1)
          echo "RELEASEMSG<<EOF" >> $GITHUB_ENV
          git log --pretty=format:%h\ %s $prevtag..$lasttag  >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1.1.4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: ${{ env.RELEASEMSG }}
          draft: false
          prerelease: true
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./test.pdf
          asset_name: test.pdf
          asset_content_type: application/pdf
